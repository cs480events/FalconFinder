package com.company;


public class Main {
    public static void main(String[] args) {
//        //Insert Other Events to the database

//        OtherEventList list = OtherEventList.getInstance();
//        for(Event t:list.getList()){
//            if (t.getDate().isEqual(LocalDate.now()) || t.getDate().isAfter(LocalDate.now())) {
//          SQLHelper.insertData(t.createSQL());}
//       }

//
        //Insert Athletics Events to the database

//        AthleticsEventList ls = AthleticsEventList.getInstance();
//        for (Event s : ls.getList()) {
//            if (s.getDate().isEqual(LocalDate.now()) || s.getDate().isAfter(LocalDate.now())) {
//            SQLHelper.insertData(s.createSQL());
//            }
//        }


        //Insert Career Events to the database
//        CareerEventList cs = CareerEventList.getInstance();
//        for (Event j : cs.getList()) {
//            if (j.getDate().isEqual(LocalDate.now()) || j.getDate().isAfter(LocalDate.now())) {
//            SQLHelper.insertData(j.createSQL());}
//        }





//        try {
//            // Get different types of events that is in the current month and on or after today.
//            ArrayList<DatabaseEvent> all = SQLHelper.getEvent(SQLHelper.ALL);
//              ArrayList<DatabaseEvent> myevent = SQLHelper.getEvent(SQLHelper.C);

//
//            ArrayList<DatabaseEvent>athletics = SQLHelper.getEvent(SQLHelper.ATHLETICS);
//            athletics.stream().forEach(System.out::println);
//
//            ArrayList<DatabaseEvent> career = SQLHelper.getEvent(SQLHelper.CAREERS);
//            career.stream().forEach(System.out::println);
//
//            ArrayList<DatabaseEvent> other = SQLHelper.getEvent(SQLHelper.OTHER);
//            other.stream().forEach(System.out::println);
//
//        } catch (Exception e) {
//            e.printStackTrace();
//        }
}
}